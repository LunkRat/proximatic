http:
  routers:
    hackernews:
      entryPoints:
        - "websecure"
      rule: "Host(`hackernews.proximatic.link`)"
      middlewares:
        - default-headers
      tls:
        {}
      service: hackernews

  services:
    hackernews:
      loadBalancer:
        servers:
          - url: "https://news.ycombinator.com"
        passHostHeader: false

  middlewares:

    default-headers:
      headers:
        frameDeny: true
        sslRedirect: true
        browserXssFilter: true
        contentTypeNosniff: true
        forceSTSHeader: true
        stsIncludeSubdomains: true
        stsPreload: true

